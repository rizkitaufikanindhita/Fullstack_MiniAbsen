import React from "react";

const NotFound = (props) => {
  return (
    <div className="items-center justify-center mt-10 text-4xl font-bold text-center">
      {props.title}
    </div>
  );
};

export default NotFound;
